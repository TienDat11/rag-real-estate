---
scope: rag-real-estate MVP — RAG pháp lý bất động sản (LightRAG + FastAPI + PostgreSQL)
purpose: build-substrate
altitude: feature
updated: 2026-08-10T21:20
---

- (event by coach) run started — bmad-architecture Fast path (user: 'lựa chọn architecture cho tôi rồi ghi vào file để chỉnh sửa plan')
- (constraint by user) user: 'cần giá rẻ thôi, viettel thấy giá chát quá' — deploy ưu tiên chi phí thấp, KHÔNG premium managed vDBS
- (decision by coach) deploy MVP = Viettel Cloud VPS 2-4vCPU/4GB (~235-315k/tháng) tự quản PG cùng box (PGTableGraphStorage plain PG) — rẻ hơn PowerNet (~292k) VÀ uy tín hơn (tập đoàn nhà nước, DC 02 Quang Trung ĐN); defer managed vDBS
- (decision by coach) thêm 3 khối mới từ research: (1) doc lifecycle state machine draft→review→approved→published→deprecated→deleted — chỉ published vào index, deleted = cascade delete chunks+embed; (2) RBAC role model 7 vai + uploader!=approver (SSD NIST); (3) retrieval-time ACL = PG RLS trên chunk table (FORCE RLS, NOBYPASSRLS, SET LOCAL identity, fail-closed)
- (decision by coach) guardrails 4 lớp chống prompt injection (indirect = rủi ro #1): L1 Llama Prompt Guard 2 (22M, 19ms CPU) screen input+chunks — test tiếng Việt trước khi tin; L2 instruction hierarchy + delimiters/JSON-encode, cấm concat retrieved vào system prompt; L3 chunk limits + hash/provenance allowlist; L4 output grounding + audit log replayable
- (decision by coach) content-addressed incremental update: chunk ID ổn định {doc_id}:{chunk_index} + content-hash change detection; đổi embedding model = full rebuild schema-migration (never incremental), version embedding per row + parallel index
- (decision by coach) eval van hanh: golden set version cung corpus (golden_set_vN.json) + re-baseline sau moi update 6 thang (eval invalidation paradox); PR gate = faithfulness + answer-relevancy subset, nightly = full 4 metric; threshold tu baseline; faithfulness floor 0.96 regulated (single-source, can calibrate)
- (decision by coach) env separation: dev/staging/prod tach biet, cam ghi prod DB tu test; audit log replayable (query-retrieval-prompt-answer); secrets qua env (AIBOX_API_KEY)
- (assumption by coach) ASSUMPTION: gia VPS Viettel ~235-315k tu AI-synth single-source — bat buoc quote vendor truoc khi chot; MVP single-tenant (chua can multi-tenant org)
- (event by coach) gap reconciliation xong: plan .claude/plans/rag-real-estate-mvp.plan.md them 6 dong ADR moi (roles/RBAC, PG RLS, guardrails 4 lop, eval versioning, deploy) + section 'Gap vs architecture spine' + impact 3 phase; 3 gap lon dong: roles quan ly tai lieu, access control, deploy/provider, chong prompt injection
- (event by coach) reviewer gate: lint PASS (0 finding); tech-verify PASS-WITH-WARNINGS (fix PG 16.6+, Ubuntu 24.04, unpinned marks, AD-8 caveat); adversarial FAIL-recoverable -> fix 5/5: AD-4 per-command RLS + rowcount assert, AD-7 content-stable ID doc_id:version:index + citation pin hash, AD-3 snapshot-lock sau approved, AD-5 publisher!=approver!=uploader DB constraint, AD-9 env topology 1-box same-PG; them AD-11 eval role, AD-12 graph ACL
- (event by coach) spine finalized (status: final, 12 AD)
- (decision) Rationale SEP: sếp đề xuất Pinecone + OpenAI embedding (như docs cũ). Lý do chọn PG single-backend thay Pinecone: (1) LightRAG cần graph storage — PGTableGraphStorage lưu entity/relation, Pinecone CHỈ vector -> vẫn phải thêm PG cho graph = 2 hệ thống; (2) Pinecone free tier giới hạn us-east-1 (latency VN->US ~250ms, dữ liệu pháp lý sang Mỹ), vượt -> Standard min 0/thang ~1.3tr VNĐ đắt hơn cả VPS; (3) backup pg_dump 1 lệnh. Embedding: aibox v4 (1024, tiếng Việt chuẩn, VNĐ, trong nước) vs OpenAI 3-small (/usr/bin/bash.02/1M, 1536, MTEB 62.3% tiếng Anh, tiếng Việt kém)
- (decision) PA trung gian nếu sếp cứng rắn: PG giữ graph leg + Pinecone cho vector leg (nếu sếp ĐANG trả phí Pinecone sẵn) — nhưng khuyến nghị KHÔNG: 2 hệ thống phức tạp backup + LightRAG PGTableGraphStorage thiết kế single-backend. Embedding: nếu sếp muốn OpenAI, cân nhắc giữ aibox vì tiếng Việt + data residency, nhưng có thể chấp nhận được nếu công ty đã có OpenAI API key
- (decision) AD-18 (ADOPTED by user 2026-08-10): orchestration layer = LlamaIndex Workflows (llama-index-core 0.14.23, event-driven @step, async, bundled workflows) boc ngoai cac step da thiet ke; LightRAG 1.5.6 GIU lam retrieval core (khong thay the). User chon vi uu tien 'ca co san, nhanh nhat'; LangChain/LangGraph bi loai (overkill + churn tutorial 0.x). Chan SQL: spec-builder deterministic la PRIMARY (affordability/filter); NL2SQL NLSQLRetriever return_raw=True dua vao MVP nhu route thu hai gated bang detector intent deterministic + day du guardrail (ro_nl2sql read-only, sqlglot AST 1-SELECT, whitelist v_unit_offers, sample_rows=0, LIMIT wrap, statement_timeout connect_args). Binds: api/ orchestration + sql_leg + requirements. Prevents: 2 framework tranh nhau quyen orchestration; NL2SQL bypass allowlist; QueryPipeline (deprecated) duoc dung.
- (constraint) Plan v2.1 phai self-contained cho session moi dung basesource BE+FE: file tree + requirements pin + env contract + lenh verify tung ngay + API contract + SSE schema + web/ spec. Spike Ngay 1-2 bat buoc: verify Workflows event API (send_event/collect_events) + NLSQLRetriever metadata['result']/['sql_query'] + pip check pinned set. Neu Workflows API khong khop -> fallback asyncio.gather trong step, khong block MVP.
