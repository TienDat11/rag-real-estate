# =============================================================================
# rag-real-estate — Environment template (KHÔNG commit giá trị thật)
# Plan §16.6 — copy sang .env và điền giá trị
# =============================================================================

# --- Postgres (LightRAG đọc trực tiếp POSTGRES_*) ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=ragre
POSTGRES_PASSWORD=__CHANGE__
POSTGRES_DATABASE=ragre
POSTGRES_MAX_CONNECTIONS=10
POSTGRES_VECTOR_INDEX_TYPE=HNSW
POSTGRES_HNSW_M=16
POSTGRES_HNSW_EF=64
LIGHTRAG_WORKSPACE=ragre_mvp

# --- Lingua / embedding ---
EMBEDDING_BINDING=dashscope        # dashscope|aibox|local
EMBEDDING_API_KEY=__CHANGE__
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v4
EMBEDDING_DIM=1024

# --- Rerank ---
RERANK_BINDING=dashscope           # dashscope|aibox|null — aibox dùng RERANK_BASE_URL + /v1/rerank
RERANK_API_KEY=__CHANGE__
RERANK_BASE_URL=__CHANGE__
RERANK_MODEL=qwen3-rerank
ENABLE_RERANK=true

# --- Geo / Places (nearby amenities — default static fallback brief §7) ---
GEO_BINDING=static                 # static|google|off — google cần GEO_API_KEY + GEO_BASE_URL
# GEO_API_KEY=                    # Google Places API key (chưa có → static fallback)
# GEO_BASE_URL=https://maps.googleapis.com/maps/api/place
# GEO_RADIUS_M=10000              # bán kính tìm kiếm (mét)
# GEO_STATIC_PATH=db/seed/static_places.json
# GEO_CENTER_LAT=16.0558          # tâm dự án (THE CAMELLIA, Sơn Trà — approx)
# GEO_CENTER_LNG=108.2455

# --- LLM gateway (OpenAI-compatible) ---
LLM_API_KEY=__CHANGE__
LLM_BASE_URL=__CHANGE__
LLM_MODEL_REWRITE=deepseek-v4-flash
LLM_MODEL_EXTRACT=qwen3.7-flash
LLM_MODEL_ANSWER=deepseek-v4-flash
LLM_MODEL_ANSWER_PRO=deepseek-v4-pro
LLM_MODEL_GUARD=deepseek-v4-flash-0731
LLM_MODEL_NL2SQL=qwen3.7-flash

# --- Query budget (tokens) ---
QUERY_MAX_ENTITY_TOKENS=2000
QUERY_MAX_RELATION_TOKENS=2000
QUERY_MAX_TOTAL_TOKENS=6000

# --- Guard (L1) ---
# GUARD_INPUT_PG2_URL=     # optional Prompt Guard 2 /text/check endpoint (api/guard_input.py)

# --- Ingest (defaults trong api/config.py) ---
# CHUNK_CAP=1200           # hard cap mỗi chunk
# EXTRACT_TIMEOUT=90.0     # giây mỗi lần extraction
# MAX_ASYNC_LLM=6
# MAX_PARALLEL_WORKERS=2

# --- App ---
APP_ENV=dev
# CORS_ORIGINS=["http://localhost:3000"]   # JSON list — mặc định localhost:3000
